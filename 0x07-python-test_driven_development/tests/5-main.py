#!/usr/bin/python3

from text_indentation import text_indentation

text = """This is a sample text. It includes multiple sentences. Each sentence ends with a separator: . or ? or :"""
text_indentation(text)
